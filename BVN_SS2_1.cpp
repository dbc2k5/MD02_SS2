#include<stdio.h>
int main(){
	float lenght = 8.5;
	float width = 4.5;
	float sell = 5.5;
	float PI = 3.14;
	int a, b, r;
	int C_SQUARE = (a*4);
	int S_SQUARE = (a*a);
	int C_RECTANGULAR = ((a+b)*2);
	int S_RECTANGULAR = (a*b);
	int C_CIRCLE = (2*PI*r);
	int S_CIRCLE = (2*PI*r*r); 
}
